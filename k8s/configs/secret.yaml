apiVersion: v1
kind: Secret
metadata:
  namespace: alert-dispatcher
  name: alert-dispatcher-secrets
type: Opaque
data:
  # Base64 encoded values - replace with your actual base64 encoded secrets
  # echo -n "your-sqs-queue-url" | base64
  SQS_QUEUE_URL: ""
  # echo -n "xoxb-your-bot-token" | base64
  SLACK_BOT_TOKEN: ""
  # echo -n "your-signing-secret" | base64
  SLACK_SIGNING_SECRET: ""
  # echo -n "your-webhook-url" | base64 (optional)
  SLACK_WEBHOOK_URL: ""

---
